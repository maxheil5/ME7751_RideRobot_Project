# Optional URDF path (disabled by default).
# Uncomment to enable mesh robot rendering.
# VITE_URDF_URL=/assets/robot_urdf/kuka_iiwa/model_free_base.urdf

# Optional override root used to resolve package:// URIs in URDF mesh paths.
# Default (if unset in code) is /assets/robot_urdf/kuka_iiwa
# VITE_URDF_PACKAGES=/assets/robot_urdf/kuka_iiwa
